import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttonContainer: {
    height: 125,
    width: 125,
    borderRadius: 125 / 2,
    alignItems: 'center',
    justifyContent: 'center',
    borderColor: '#ffffff',
    borderWidth: 2,
  },
  textStyle: {
    color: '#ffffff',
    fontSize: 22,
    fontWeight: 'bold',
  },
});
